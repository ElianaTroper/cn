
function initPage() {
	// TODO: Add a text input box, and a holder below
	// TODO: Add a warning
	// TODO: Set the input box to send a message to the proper pubsub when not empty and onsubmit is hit
}

export async function runPost(node, params) {
	initPage()
}
